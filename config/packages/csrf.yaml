# Stateless CSRF для login/logout; формы лендинга — с обычным токеном в HTML.
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - authenticate
            - logout
