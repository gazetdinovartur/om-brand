{% for entry in entries %}
    {% include 'web/chronicle/_entry.html.twig' with {
        entry: entry,
        featured: markLiked|default(false),
        hubQuery: hubQuery|default({})
    } %}
{% endfor %}
